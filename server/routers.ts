// const router = require('express').Router();
// module.exports.routers = router;

// router.get('/', (req: any, res: any) => { res.send([{message: 'hello world'}]); });
// router.get('/users', (req: any, res: any) => { res.send([])});
// router.post('/users', (req: any, res: any) => { res.send({body: req.body})});